{
  "compilerOptions": {
    "target": "es6", // 컴파일된 코드가 실행될 ECMAScript 버전
    "module": "ESNext", // 사용할 모듈 시스템
    "outDir": "./dist", // 컴파일된 파일들이 저장될 디렉토리
    "rootDir": "./src", // 소스 파일들이 있는 디렉토리
    "strict": true, // 엄격한 타입 체킹 옵션 활성화
    "esModuleInterop": true, // CommonJS 모듈과 ES6 모듈 간 호환성 향상
    "forceConsistentCasingInFileNames": true, // 파일 이름의 대소문자 일관성 강제
    "moduleResolution": "node",
    "skipLibCheck": true, // 성능 향상을 위해 모든 선언 파일(`*.d.ts`)의 타입 체크를 생략
    "lib": ["es2015"] // 컴파일러에게 포함시킬 라이브러리 목록
  },
  "include": ["src/server.ts", "src/**/*.ts"],
  "exclude": ["node_modules"]
}
